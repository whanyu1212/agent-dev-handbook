# Ask Mode Rules (Non-Obvious Only)

- **Documentation Source**: Canonical guides are in `src/agent_dev_handbook/adk/notes/` (Markdown files), NOT in a separate docs folder.
- **Example Authority**: Trust code in `src/agent_dev_handbook/adk/examples/` over comments.
- **Naming Confusion**: `test_dynamic_multiturn.py` is an EXAMPLE SCRIPT, not a unit test.
- **Empty Directories**: `shared/utils` appearing empty is a known state, not a loading error.
